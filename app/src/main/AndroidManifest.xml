<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gsthub">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ProfileTeacher"></activity>
        <activity android:name=".CreateProfileGuest" />
        <activity android:name=".CreateProfileAlumni" />
        <activity android:name=".CreateProfileTeacher" />
        <activity android:name=".CreateProfileStudent" />
        <activity android:name=".SignIn" />
        <activity android:name=".CreateAccAlumni" />
        <activity android:name=".CreateAccGuest" />
        <activity android:name=".CreateAccStudent" />
        <activity android:name=".CreateAccTeacher" />
        <activity android:name=".SignInAs" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>